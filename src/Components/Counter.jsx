import React from 'react'

function Counter() {
  
  
  const increCount = () => {
  setCount(count => count + 1);
  
const reset = () => 
}

const decreCount = () =>{
  if (count>0)
  setCount(count => count - 1);
}
  
    return (
    <>
      
    </>
  )
}

export default Counter
